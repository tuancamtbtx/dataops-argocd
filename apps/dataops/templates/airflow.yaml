apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow
  namespace: argocd
spec:
  destination:
    namespace: airflow
    server: https://kubernetes.default.svc
  project: default
  source:
    path: values/dataops/airflow
    repoURL:https://github.com/tuancamtbtx/dataops-argocd
  syncPolicy:
    syncOptions:
      - CreateNamespace=true